[project]
name = "mini-llm"
version = "0.1.0"
description = "One-command setup for a headless Mac Mini AI server"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
]

[tool.uv.workspace]
members = [
    "mini-tools/cue",
]
